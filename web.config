<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
<rule name="Static Files" stopProcessing="true">
          <match url="^(.*\.(css|js|png|jpg|jpeg|gif|ico|svg))$" />
          <action type="None" />
        </rule>
        <rule name="PHP Files" stopProcessing="true">
          <match url="^(.*\.php)$" />
          <action type="Rewrite" url="{R:1}" />
        </rule>
        <rule name="Tenant Dashboard" stopProcessing="true">
          <match url="^tenant_dashboard.php" />
          <action type="Rewrite" url="tenant_dashboard.php" />
        </rule>
        <rule name="Tenant Login" stopProcessing="true">
          <match url="^tenant_login.php" />
          <action type="Rewrite" url="tenant_login.php" />
        </rule>
      </rules>
    </rewrite>
  </system.webServer>
</configuration>

