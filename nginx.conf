# Updated block for your OralSync Mobile API
location ~ ^/api/(.+\.php)$ {
    fastcgi_split_path_info ^(.+\.php)(/.*)$;
    fastcgi_pass 127.0.0.1:9000;
    fastcgi_index index.php;
    include fastcgi_params;

    # Explicitly pass the script filename for Azure
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    
    # Crucial for mobile POST requests
    fastcgi_param REQUEST_METHOD $request_method;
    fastcgi_param CONTENT_TYPE $content_type;
    fastcgi_param CONTENT_LENGTH $content_length;
}

# This block ensures ALL .php files, regardless of the folder, 
# are sent to the PHP processor for ALL request methods (GET, POST, etc.)
location ~ [^/]\.php(/|$) {
    fastcgi_split_path_info ^(.+?\.php)(/.*)$;
    fastcgi_pass 127.0.0.1:9000;
    fastcgi_index index.php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    fastcgi_param PATH_INFO $fastcgi_path_info;
}

location / {
    index index.php index.html index.htm;
    try_files $uri $uri/ /index.php$is_args$args;
}