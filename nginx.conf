server {
    root /home/site/wwwroot;
    index index.php index.html;

    # 1. Handle the URL routing (Web Portal & API)
    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    # 2. THE MASTER PHP BLOCK
    # This matches any .php file, whether in root or in /api/
    location ~ [^/]\.php(/|$) {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        include fastcgi_params;

        # Maps the URL path to the actual folder on Azure
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # DATA PASS-THROUGH (Fixes "Credentials Required")
        fastcgi_param REQUEST_METHOD  $request_method;
        fastcgi_param CONTENT_TYPE    $content_type;
        fastcgi_param CONTENT_LENGTH  $content_length;
    }
}