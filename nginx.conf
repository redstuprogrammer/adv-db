server {
    # ... keep your listen and server_name lines at the top ...

    root /home/site/wwwroot;
    index index.php index.html;

    # 1. Handle the main landing pages and static files
    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    # 2. THE MASTER PHP BLOCK (Handles both Web and Mobile API)
    location ~ [^/]\.php(/|$) {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        include fastcgi_params;

        # This line maps the URL to the physical file on the Azure disk
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # THESE ARE THE FIXES FOR YOUR MOBILE "CREDENTIALS REQUIRED" ERROR
        fastcgi_param REQUEST_METHOD  $request_method;
        fastcgi_param CONTENT_TYPE    $content_type;
        fastcgi_param CONTENT_LENGTH  $content_length;
    }
}