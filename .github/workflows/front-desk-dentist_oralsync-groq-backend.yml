name: "Deploy: oralsync-groq-backend (disabled)"

on:
  workflow_dispatch: {}

# Disabled by default to prevent accidental runs; keep for history.
if: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled."
